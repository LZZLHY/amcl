diff --git a/hotspot/make/linux/makefiles/gcc.make b/hotspot/make/linux/makefiles/gcc.make
index 70b130b2..7947eaf8 100644
--- a/hotspot/make/linux/makefiles/gcc.make
+++ b/hotspot/make/linux/makefiles/gcc.make
@@ -388,3 +388,7 @@ ifndef USE_SUNCC
 endif
 
 -include $(HS_ALT_MAKE)/linux/makefiles/gcc.make
+
+# OHOS (AMCL): JDK 8 calls member fns through possibly-NULL this with internal
+# null guards; clang deletes them as UB -> SIGSEGV. Preserve null checks.
+CFLAGS += -fno-delete-null-pointer-checks
diff --git a/hotspot/src/cpu/aarch64/vm/immediate_aarch64.cpp b/hotspot/src/cpu/aarch64/vm/immediate_aarch64.cpp
index 9f606ff3..cb643513 100644
--- a/hotspot/src/cpu/aarch64/vm/immediate_aarch64.cpp
+++ b/hotspot/src/cpu/aarch64/vm/immediate_aarch64.cpp
@@ -20,6 +20,7 @@
  */
 
 #include <stdlib.h>
+#include <pthread.h>
 #include "immediate_aarch64.hpp"
 
 // there are at most 2^13 possible logical immediate encodings
@@ -126,7 +127,11 @@ u_int64_t replicate(u_int64_t bits, int nbits, int count)
   // nbits may be 64 in which case we want mask to be -1
   u_int64_t mask = ones(nbits);
   for (int i = 0; i < count ; i++) {
-    result <<= nbits;
+    // OHOS (AMCL): nbits can be 64 here (expandLogicalImmediate i=5 path), and
+    // shifting a 64-bit value by 64 is UB. When nbits>=64, count==1 so result is
+    // still 0 and the shift is semantically a no-op; skipping it kills the UB that
+    // corrupted the aarch64 -O3 table entry for 0xFFFFFFFFFFFFFFF0.
+    if (nbits < 64) result <<= nbits;
     result |= (bits & mask);
   }
   return result;
@@ -240,7 +245,14 @@ int expandLogicalImmediate(u_int32_t immN, u_int32_t immr,
 
 // constructor to initialise the lookup tables
 
-static void initLITables() __attribute__ ((constructor));
+// OHOS (AMCL): the custom ELF loader does NOT run libjvm.so .init_array, so the
+// original __attribute__((constructor)) never fired and the logical-immediate
+// lookup table stayed empty -> every `andr/orr/eor` immediate failed to encode
+// (guarantee "Field too big for insn" in generate_call_stub). Build it lazily on
+// first lookup via pthread_once; first use is during single-threaded VM init.
+static void initLITables();
+static pthread_once_t li_once_control = PTHREAD_ONCE_INIT;
+static void ensureLITables() { pthread_once(&li_once_control, initLITables); }
 static void initLITables()
 {
   li_table_entry_count = 0;
@@ -263,11 +275,13 @@ static void initLITables()
 
 u_int64_t logical_immediate_for_encoding(u_int32_t encoding)
 {
+  ensureLITables();
   return LITable[encoding];
 }
 
 u_int32_t encoding_for_logical_immediate(u_int64_t immediate)
 {
+  ensureLITables();
   struct li_pair pair;
   struct li_pair *result;
 
diff --git a/hotspot/src/share/vm/oops/markOop.hpp b/hotspot/src/share/vm/oops/markOop.hpp
index 649217c8..cd06bab3 100644
--- a/hotspot/src/share/vm/oops/markOop.hpp
+++ b/hotspot/src/share/vm/oops/markOop.hpp
@@ -104,7 +104,16 @@ class JavaThread;
 class markOopDesc: public oopDesc {
  private:
   // Conversion
-  uintptr_t value() const { return (uintptr_t) this; }
+  uintptr_t value() const {
+    // OHOS (AMCL): markOop is a tagged "fake pointer"; its low bits ARE part
+    // of the mark word. clang assumes markOopDesc* is 8-aligned and folds the
+    // low-bit predicates (has_locker/has_monitor/is_neutral) to constants,
+    // breaking ObjectSynchronizer::inflate(). Launder through empty asm so the
+    // compiler cannot assume alignment of these bits.
+    uintptr_t v = (uintptr_t) this;
+    __asm__("" : "+r"(v));
+    return v;
+  }
 
  public:
   // Constants
