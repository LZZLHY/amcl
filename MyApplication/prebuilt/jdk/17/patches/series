# MC-OHOS OpenJDK 17u patch series
# 标准 .patch 文件（git diff 格式），按顺序用 git apply 应用。
#
# 应用方式：bash docker/apply_patches.sh --patch-dir prebuilt/jdk/17/patches --target /build/jdk17u
# 验证方式：每个 patch git apply --check 通过 → 全应用 → gmake hotspot CONF_CHECK=ignore
#
# 2026-05-15 P6.5: 物理位置从 docker/patches/jdk17u/ 迁到此处；
#                  并补全了 0005-0008 4 个 OHOS 运行时 patch（之前在 ohos-debug
#                  容器里活着、但没归档到 series；本次评审从 /build/jdk17u
#                  git diff 提取后入仓）。

# === 1-4: musl libc 与 OHOS 沙箱适配 ===

# musl libc: 防止 dlvsym 重定义 + 跳过不支持的 dlinfo()
0001-musl-dlvsym-dlinfo.patch

# OHOS 沙箱: JAVA_HOME 路径推断失败，优先使用环境变量
0002-java-home-env.patch

# OHOS 沙箱: dll_dir 同理，优先使用 SUN_BOOT_LIBRARY_PATH
0003-dll-dir-env.patch

# musl libc: 跳过不支持的 setutxent/getutxent/endutxent
0004-musl-utmpx.patch

# === 5-8: OHOS 运行时关键 patch（HotSpot 信号处理 / Safepoint / libjli） ===

# OHOS：javaSignalHandler 不在未识别信号上 abort，避免设备误杀 JVM
0005-signals-posix-abort-if-unrecognized.patch

# OHOS：aarch64 信号处理对 ELF loader 加载的代码做 SafePoint polling SEGV fallback
# （CodeCache 看不到 ELF-loaded code，需要在 SEGV 时 disarm polling page 让 CPU 重试）
0006-aarch64-elf-safepoint-fallback.patch

# OHOS：safepoint polling page 用 MEM_PROT_READ 而不是 MEM_PROT_NONE
# 配合 0006 的 fallback 一起，让 polling 走 SIGSEGV→重试，避免 SIGBUS abort
0007-safepoint-mechanism-mem-prot-read.patch

# OHOS：libjli 跳过 re-exec 路径（RequiresSetenv 永远返回 JNI_FALSE）
# ELF loader 已经处理好了 LD_LIBRARY_PATH，原路径是为 musl 早期版本的兜底，会触发死锁
0008-libjli-skip-re-exec.patch

# === 9: OHOS 沙箱下 IPv6 能力误判（路线 A / 方案 §四·五 R4） ===

# 与 21/25 的同名 patch **语义相同、代码形状不同**：jdk17u 的 IPv6_supported() 里
# `#ifdef __linux__` 块内每个 `return JNI_FALSE` 前都要 `close(fd)`（21/25 把 close 提到了
# socket 检查之后），所以回落分支也必须 close。生成脚本按这两种形状自动识别，不靠版本号猜。
# 另：17 还多一条 21/25 已删掉的判据 —— `getsockname(fd 0)`（inetd/xinetd 启动检测）。
#
# OHOS：应用沙箱对 /proc/net/if_inet6 返回 EACCES，而上游用「能不能读到该文件第一行」回答
# 「本机有没有 IPv6」⇒ Net.isIPv6Available() 恒 false ⇒ NIO 只建 IPv4-only channel ⇒
# 连 Inet6Address 在 Net.checkAddress 抛 UnsupportedAddressTypeException（message 为 null）。
#
# patch 语义（⚠️ 改它之前先读）：
#   - **不删判据**。fopen 仍先试；文件可读时与上游逐字节一致（平台放开后自动退场）。
#   - 只在 errno == EACCES / EPERM 时回落 getifaddrs() 真实探测 ——「读不到」是缺一次观测。
#   - 回落**同时要求 dlsym(inet_pton) 非空**：本条 patch 的目标是给出「文件可读时上游会给的
#     答案」，而那条路径上这个 API 可用性判据仍然成立。跳过它会让回落比它所替代的代码更宽松。
#   - **ENOENT 刻意不回落**：文件真不存在＝内核没开 IPv6，那正是上游判据成立的场合。
#   - 回落把 loopback（::1）算作「有 IPv6」，与「文件可读时上游会给的答案」保持一致。
#   - helper 是 static，不新增导出符号。
#
# 验证（2026-08-29）：
#   同树基线对照（撤掉本 patch 增量重编再逐项比）：导出符号 89 → 89 **逐个相同**、
#   DT_NEEDED 一致、未定义引用只多 getifaddrs/freeifaddrs、两边都仍引用 fopen。
#   与**出货件**（v17.0.13-ohos-4 包内 lib/libnet.so，108520 B，
#   sha256 dedb356b…）比：导出符号 89 个**逐个相同**、DT_NEEDED 一致 ⇒ 可作掉落式替换。
#   ⚠️ 但 baseline（112656 B）与出货件**不逐字节相同**。差异存在于「出货件 vs 我的 baseline」
#     ——两者都不带本 patch，所以差异全部来自构建环境（出货那次是 2026-03 构建，且该包**没有
#     strip**，两边都带 .symtab/.debug_*），与本 patch 无关。
#     对照：21 与 25 的 baseline 与各自出货件**逐字节相同**（见它们的 series）。
#     ⇒ **17 的构建当前不是位级可复现的**，这是一条先于本 patch 存在的缺口。
#
# ⚠️ 构建注意（2026-08-29 实测）：
#   1) 17 的 configure **拒绝** `--openjdk-target` 与 autoconf `--build` 并用
#      （"Specifying --openjdk-target together with autoconf --build ... is not allowed"），
#      要用纯 autoconf 写法 `--build=x86_64-unknown-linux-gnu --host=aarch64-unknown-linux-musl`。
#   2) 必须显式指定 `--build`：17 的 make/autoconf/build-aux/config.guess 带「Test and fix wsl」
#      判定，Docker Desktop 的容器跑在 WSL2 里会被判成 pc-wsl ⇒ 启用 fixpath ⇒ configure
#      exit 127。**也就是说 docker/build_jdk17_ohos.sh 今天在这台机器上跑不过。**
#   3) boot JDK 必须是 16 或 17（不是越新越好）。
0010-ipv6-supported-sandbox-fallback.patch
